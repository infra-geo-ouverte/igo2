@require '../../css/theme.styl';
@require '../../css/media.styl';

:host, .igo-map-target {
  width: 100%;
  height: 100%;
}

:host >>> .ol-zoom {
  top: $igo-margin;
  right: $igo-margin;
  left: initial;

  +media(mobile) {
    display: none;
  }
}
