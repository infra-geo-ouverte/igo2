@require '../css/theme.styl';

:host, main {
  width: 100%;
  height: 100%;
}

main {
  position: fixed;
  padding: 0;
  transition: 0.5s;
}

/*--- Spinner ---*/
igo-spinner {
  position: absolute;
  top: $igo-margin;
  right: $igo-margin;
  z-index: 5;
}
