@require '../css/base.styl';

igo-header {
  height: $b-height-header;
}

#router-body {
  // margin-left: $b-width-navbar;
  position: fixed;
  width: 100%;
  height: "calc(%s + 7px)" % $b-height-header;
  padding: 0;
  transition: 0.5s;
}
