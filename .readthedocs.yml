# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

# Build documentation in the docs/ directory with Sphinx
sphinx:
   configuration: docs/conf.py
   ###fail_on_warning: true
# Optionally build your docs in additional formats such as PDF
formats:
   - pdf

# Optionally set the version of Python and requirements required to build your docs
python:
   version: "3.7"
   install:
      - method: pip
        docutils==0.17
      #   path: docutils==0.17
#         /home/docs/checkouts/readthedocs.org/user_builds/igo2/envs/newqueryparam-workspace-and-wmswfscombine/bin/python -m pip install --upgrade --upgrade-strategy eager --no-cache-dir ./docutils==0.17
# ERROR: Invalid requirement: './docutils==0.17'
# Hint: It looks like a path. File './docutils==0.17' does not exist.
   # - requirements: docs/requirements.txt